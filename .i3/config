# This file has been auto-generated.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# See http://i3wm.org/docs/userguide.html for a complete reference

set $mod Mod4

set $active #8a29ff
set $focused #fe5817
set $unfocused #553b66
set $transparent #00000000

# Client colors
#              border  backgr  text    indicator
client.focused $focused $focused #ffffff #0000ff
client.focused_inactive $active $active #ffffff #0000ff
client.unfocused #846796 $unfocused  #ffffff #0000ff

# App-specific color settings
for_window [class="Chromium-browser"] title_format "<span foreground='#ffc1df'>%title</span>"
for_window [class="Google-chrome"] title_format "<span foreground='#59dff7'>%title</span>"
for_window [class="Xfce4-terminal"] title_format "<span foreground='#7fff83'>%title</span>"

focus_follows_mouse yes 
# Disabled so rofi appears on "active" monitor
# mouse_warping none

# Font for window titles. 
font pango:FontAwesome, DejaVu Sans Mono 8
font pango:DejaVu Sans Mono 20px

# borders
new_window pixel 2

# Sound hotkeys for Daskeyboard
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 5 +1% && killall -SIGUSR1 i3status
bindsym XF86AudioLowerVolume exec pactl set-sink-volume 5 -1% && killall -SIGUSR1 i3status
bindsym XF86AudioMute exec pactl set-sink-mute 5 toggle && killall -SIGUSR1 i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# App launching hotkeys
bindsym $mod+Return exec urxvtc-256color
bindsym $mod+C      exec google-chrome

# screenshotting
bindsym Print      exec xfce4-screenshooter

# kill focused window
bindsym $mod+q kill

# start app launcher
bindsym $mod+d exec "i3-dmenu-desktop --dmenu='rofi -show combi 'run, window''"

# vim bindings for basic window management
bindsym $mod+h		focus left
bindsym $mod+Shift+h	move left 30

bindsym $mod+j		focus down
bindsym $mod+Shift+j	move down 30

bindsym $mod+k		focus up
bindsym $mod+Shift+k	move up 30

bindsym $mod+l		focus right
bindsym $mod+Shift+l	move right 30

# Keep arrow keys bound 
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Arrow bindings for movement as well
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split horizontal or vertical orientation
# bindsym $mod+h split h - tbd: used in vim bindings
# bindsym $mod+v split v - tbd
bindsym $mod+t split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen
# change container layout (tabbed, toggle split)
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split



# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# parent / child container focus
bindsym $mod+a focus parent
bindsym $mod+z focus child

# Workspaces
set $ws1 "1 "
set $ws2 "2  "
set $ws3 "3 "
set $ws4 "4 "
set $ws5 "5 "
set $ws6 "6 "
set $ws7 "7 "
set $ws8 "8 "
set $ws9 "9 "
set $ws10 "10 "


# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# lock
bindsym $mod+x exec i3lock -c 000000

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Do you really want to logout? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

	# resize fast with page keys
	bindsym Next resize shrink height 50px or 50 ppt
	bindsym Prior resize grow height 50px or 50 ppt


        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# primary bar
bar {

	tray_output DVI-0
	output HDMI-0
	colors {
       		background $transparent
	        separator #00ff00
	        statusline #b3ffb3
	        focused_workspace $focused $focused #ffffff
	        active_workspace $active $active #ffffff
	        inactive_workspace #5f5f5f $unfocused #808080
###	        urgent_workspace #2f343a #900000 #ffffff
	}
	position bottom
}

# secondary screen for primary bar
# colors have to be duplicated, unfortunately
bar {

	status_command i3status -c ~/.i3status.conf
	separator_symbol "|"
	output DVI-0
	colors {
       		background #191919
	        separator #00ff00
	        statusline #b3ffb3
	        focused_workspace $focused $focused #ffffff
	        active_workspace $active $active #ffffff
	        inactive_workspace #5f5f5f $unfocused #808080
	}
	position bottom

}

assign [class="^Steam"] $ws5
workspace $ws1 output HDMI-0
workspace $ws5 output HDMI-0

workspace_auto_back_and_forth yes

# pageup/pagedown for workspace cycle
bindsym $mod+Shift+Tab workspace prev_on_output
bindsym $mod+Tab workspace next_on_output
bindsym Ctrl+Tab focus right
bindsym Ctrl+Shift+Tab focus left

bindsym $mod+grave workspace back_and_forth

bindsym $mod+F2 exec renameFocusedWindow.sh

gaps inner 10
gaps outer 10

exec_always --no-startup-id feh --bg-tile ~/.config/wallpaper
exec_always --no-startup-id setxkbmap -option caps:backspace
exec_always --no-startup-id compton -f -b
exec --no-startup-id urxvtd-256color -f -q
# luke smith bindings
bindsym $mod+s		gaps inner current plus 5
bindsym $mod+Shift+s	gaps inner current minus 5

bindsym $mod+Shift+y	resize shrink width 10 px or 10ppt
bindsym $mod+Shift+u	resize shrink height 10 px or 10 ppt
bindsym $mod+Shift+i	resize grow height 10 px or 10 ppt
bindsym $mod+Shift+o	resize grow width 10 px or 10 ppt

