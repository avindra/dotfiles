#!/bin/sh

# Support for resetting Alacritty terminals
# see: https://github.com/alacritty/alacritty/wiki/Reset-Terminal

if [ -z "${ALACRITTY_LOG}" ]; then exit 1; fi

alac_pid="${ALACRITTY_LOG//[^0-9]/}"

tty=$(ps o tty= --ppid $alac_pid)

echo -e "\ec" > /dev/$tty
